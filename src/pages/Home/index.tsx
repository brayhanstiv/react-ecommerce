// Components
import { MainLayout, ProductCard } from "@/components/index";

const HomePage = () => {
  return (
    <MainLayout>
      <ProductCard />
    </MainLayout>
  );
};

export default HomePage;
